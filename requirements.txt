# ExplorerPro - Runtime-Abhängigkeiten
# Mit denselben Grenzen wie pyproject.toml.

PySide6>=6.5.0,<7.0.0
PyMuPDF>=1.21.0,<2.0.0
pandas>=2.0.0,<4.0.0
openpyxl>=3.1.0,<4.0.0

# Optionale Extras werden bewusst nicht mitinstalliert:
# pip install ".[build]"             # PyInstaller
# pip install ".[legacy-pdf]"        # PyPDF2-Fallback
# pip install ".[legacy-excel]"      # xlrd für .xls
# pip install ".[windows-shortcuts]" # pywin32 für Windows-Verknüpfungen
